package comsoc

// la fonction n'a aucun sens

// func SWFFactory(swf func(p Profile) (Count, error), tiebreak func([]Alternative) (Alternative, err error)) func(Profile) ([]Alternative, error) {
// 	return func(p Profile) (alts []Alternative, err error) {
// 		c, err := swf(p)
// 		if err == nil {
// 			bestAlts := maxCount(c)
// 			bestAlt, err := tiebreak(bestAlts)

// 		}
// 		return
// 	}
// }
